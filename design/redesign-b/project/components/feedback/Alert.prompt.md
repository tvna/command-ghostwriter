Inline status banner — confirms a generation, flags a parse error, etc.

```jsx
<Alert tone="success">CLIコマンド生成に成功</Alert>
<Alert tone="error">設定定義ファイルの読み込みに失敗</Alert>
```

Tones: `success`, `warning`, `error`, `info`. Each has a default glyph and a left accent stripe in its semantic color.
