Monospace well for CLI output, config files, and Jinja templates — the artifacts this tool produces and consumes.

```jsx
<CodeBlock title="cisco_config.toml" language="toml">{tomlSource}</CodeBlock>
```

Add `title`/`language` for the header bar with copy affordance. Omit both for a bare well.
