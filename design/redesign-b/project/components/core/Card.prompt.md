Bordered container surface — the building block of every Streamlit panel (`st.container(border=True)`).

```jsx
<Card>
  <FileUploader label="設定定義ファイルをアップロード" />
</Card>
```

Wrap uploaders, settings groups, and result wells. Override `padding` for tighter layouts.
