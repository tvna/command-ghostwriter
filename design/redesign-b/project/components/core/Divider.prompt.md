Horizontal rule. The `rainbow` variant is the brand signature — it sits directly under section subheaders.

```jsx
<h2>ファイルの組み合わせによるコマンド生成</h2>
<Divider variant="rainbow" />
```

Variants: `rainbow` (default), `subtle` (hairline), `red`.
