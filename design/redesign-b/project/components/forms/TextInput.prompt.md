Single-line text field — filenames, variable names, NaN fill characters.

```jsx
<TextInput label="ダウンロード時のファイル名" value={name} onChange={setName} />
```

Controlled via `value` + `onChange`. Gains a red focus ring.
