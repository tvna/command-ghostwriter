Pill switch for boolean settings — used throughout the 詳細設定 (advanced) tab.

```jsx
<Toggle checked={strict} label="テンプレートの変数チェック厳格化" onChange={setStrict} />
```

Controlled via `checked` + `onChange`. Knob slides onto brand red when on.
