Multi-line field and the well that displays generated CLI commands. Monospace by default.

```jsx
<TextArea label="CLIコマンド生成の出力結果" value={result} rows={12} readOnly />
```

Set `mono={false}` for prose input. Sits on the sunken code-well surface.
