# chardet 7 migration + consolidated Dependabot bumps - Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land all 31 #364 pip-group bumps plus the chardet 5->7 migration in one merge-ready PR, superseding #364.

**Architecture:** Two green commits. Commit 1 re-applies #364's already-CI-proven bumps (chardet still 5). Commit 2 performs the chardet 5->7 migration (constraint + lock + transcoder code + test expectations) so the tip is green under chardet 7.

**Tech Stack:** uv, pydantic 2.13, chardet 7, pytest (+xdist), mypy 2.x, ruff 0.15.

---

## File Structure
- `pyproject.toml` - dependency constraints (two edits: bulk bumps, then chardet).
- `uv.lock` - regenerated by `uv lock` after each constraint edit.
- `tests/unit/test_document_render.py` - pydantic error-doc URL fixture 2.11 -> 2.13.
- `features/transcoder.py` - drop ResultDict import; final fallback `return encoding` -> `return None`.
- `tests/unit/test_transcoder.py` - 10 expectation updates for chardet 7.

---

### Task 1: Re-apply #364's non-chardet bumps (commit 1, green)

**Files:**
- Modify: `pyproject.toml` (all constraints except chardet)
- Modify: `tests/unit/test_document_render.py:1302,1308`
- Regenerate: `uv.lock`

- [ ] **Step 1:** Apply these `pyproject.toml` constraint changes (chardet stays `>=5.2.0,<6`):
  - `streamlit>=1.58.0,<2`, `pyyaml>=6.0.3,<7`, `python-box>=7.4.1,<8`, `markupsafe>=3.0.3,<4`
  - pydantic comment -> 2.13 wording; `pydantic>=2.13.4,<2.14`
  - dev: `mypy>=2.1.0,<3`, `pytest-cov>=7.1.0,<8`, `types-toml>=0.10.8.20260518,<0.11`, `pre-commit>=4.6.0,<5`, `lizard>=1.23.0,<2`, `types-pyyaml>=6.0.12.20260518,<7`, `pydeps>=3.0.6,<4`, `pytest-playwright>=0.8.0,<0.9`, `ruff>=0.15.16,<0.16`, `pandas-stubs>=3.0.3.260530,<4`, `gitpython>=3.1.50,<4`, `packaging>=26.2,<27`, `pytest-mock>=3.15.1,<4`, `psutil>=7.2.2,<8`, `pytest-randomly>=4.1.0,<5`, `pytest-benchmark==5.2.3`, `pygal>=3.1.0,<4`, `pytest-item-dict>=1.3.7,<2`, `data-to-xml>=1.0.14,<2`, `vulture>=2.16,<3`, `yamllint>=1.38.0,<2`, `types-psutil>=7.2.2.20260518,<8`, `types-requests>=2.33.0.20260518,<3`, `pytest-rerunfailures>=16.3,<17`
  - local: `pudb>=2025.1.5,<2026`

- [ ] **Step 2:** Update the two pydantic URL fixtures in `tests/unit/test_document_render.py`:
  `https://errors.pydantic.dev/2.11/v/greater_than` -> `.../2.13/...` (both occurrences).

- [ ] **Step 3:** Regenerate lock: `uv lock` then verify `uv lock --check`. Expected: success.

- [ ] **Step 4:** Verify green: `uv sync --locked --group dev` then
  `uv run pytest tests/unit/test_document_render.py tests/unit/test_transcoder.py -q -p no:randomly`.
  Expected: PASS (chardet still 5).

- [ ] **Step 5:** `uv run ruff check .` and `uv run mypy features/`. Expected: clean.

- [ ] **Step 6:** Commit:
```bash
git add pyproject.toml uv.lock tests/unit/test_document_render.py
git commit -m "chore(deps): re-apply pip-group bumps and pydantic 2.13 fixture (#382)"
```

### Task 2: chardet 5 -> 7 migration (commit 2, green)

**Files:**
- Modify: `pyproject.toml` (chardet constraint)
- Modify: `features/transcoder.py`
- Modify: `tests/unit/test_transcoder.py`
- Regenerate: `uv.lock`

- [ ] **Step 1:** `pyproject.toml`: `chardet>=5.2.0,<6` -> `chardet>=7.4.3,<8`. Then `uv lock`.

- [ ] **Step 2:** `uv sync --locked --group dev`. Run `uv run pytest tests/unit/test_transcoder.py -q -p no:randomly`.
  Expected: 11 FAILS (confirms chardet 7 active). This is the red state.

- [ ] **Step 3:** Edit `features/transcoder.py`:
  - Remove lines 7-8 (the `if TYPE_CHECKING:` block importing `ResultDict`).
  - Change `result: ResultDict = chardet.detect(raw_data)` to `result = chardet.detect(raw_data)`.
  - Change the final `return encoding` (line 73) to `return None`.
  - If `TYPE_CHECKING` becomes unused, drop it from the `typing` import.

- [ ] **Step 4:** Edit `tests/unit/test_transcoder.py` expectations:
  - The 8 empty-string params: change `expected_encoding` from `"ASCII"` to `"utf-8"`:
    `encoding_convert_empty_from_shift_jis`, `_shift_jis_hyphen`, `_euc_jp`, `_euc_jp_hyphen`,
    `_utf8`, `_utf8_underscore` (in `test_transcoder_basic_functionality`);
    `encoding_convert_empty_string` (in `test_transcoder_edge_cases`);
    `binary_detect_empty_data` b"" (in `test_transcoder_binary_edge_cases`).
  - `binary_detect_large_binary`: change `expected_encoding` `"ISO-8859-1"` -> `None`.
  - `encoding_convert_music_symbol_to_shift_jis`: change `expected_result`
    `b"Hello\xe2\x99\xaaWorld"` -> `b"Hello\x81\xf4World"`.
  - Leave `binary_detect_high_ascii` unchanged (`None`).

- [ ] **Step 5:** Run `uv run pytest tests/unit/test_transcoder.py -q -p no:randomly`.
  Expected: PASS (0 failures).

- [ ] **Step 6:** `uv run ruff check . && uv run ruff format --check . && uv run mypy features/`.
  Expected: clean.

- [ ] **Step 7:** Commit:
```bash
git add pyproject.toml uv.lock features/transcoder.py tests/unit/test_transcoder.py
git commit -m "feat(transcoder): migrate to chardet 7 (#382)"
```

### Task 3: Full local verification

- [ ] **Step 1:** `uv lock --check`. Expected: up-to-date.
- [ ] **Step 2:** `uv run pytest tests/unit tests/integration -q` (skip e2e locally - browser-driven; CI runs it). Expected: PASS.
- [ ] **Step 3:** `uv run ruff check . && uv run mypy features/ app.py`. Expected: clean.

### Task 4: Push, PR, supersede #364

- [ ] **Step 1:** Push: `git push -u origin claude/pytest-parallel-markers-5mvkqk` (force-with-lease if needed; branch was reset to develop).
- [ ] **Step 2:** Open PR (base develop) citing #382, body referencing the design and the supersede of #364. ASCII only.
- [ ] **Step 3:** Comment on #364 that it is superseded by the new PR; close #364.
- [ ] **Step 4:** Subscribe to PR activity; drive CI to green. Stop before merge (maintainer merges).

---

## Self-Review
- **Spec coverage:** deps (Task 1+2), pydantic fixture (Task 1), transcoder code (Task 2), 10 test expectations (Task 2), uv.lock (both), close #364 (Task 4), verification (Task 3) - all covered.
- **Placeholder scan:** none.
- **Consistency:** chardet constraint `>=7.4.3,<8` matches investigation; expectation list (8+1+1) matches the 11 observed failures minus high_ascii (fixed by None fallback).
